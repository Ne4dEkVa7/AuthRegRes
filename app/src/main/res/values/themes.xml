<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AuthRegRes" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/soft_green</item>
        <item name="colorPrimaryVariant">@color/soft_green</item>
        <item name="android:statusBarColor">@color/soft_green</item>
    </style>

    <style name="Theme.AuthRegRes" parent="Base.Theme.AuthRegRes" />
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splashscreen_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.AuthRegRes</item>
    </style>
</resources>